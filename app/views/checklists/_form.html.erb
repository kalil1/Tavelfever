<%= form_with(model: checklist, local: true) do |form| %>
  <% if checklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checklist.errors.count, "error") %> prohibited this checklist from being saved:</h2>

      <ul>
      <% checklist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :list_name %>
    <%= form.text_field :list_name, id: :checklist_list_name %>
  </div>

  <% @activities.each do |activity| %>
    <input type="checkbox" name="activities[]" value="<%= activity.id %>"><%= activity.name %>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
